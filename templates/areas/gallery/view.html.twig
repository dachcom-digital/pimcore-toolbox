{% import '@Toolbox/macro/wrap.html.twig' as wrap_macro %}
<div class="toolbox-element toolbox-gallery {{ additionalClassesData|join(' ') }}">
    {{ wrap_macro.createWrapHead(elementThemeConfig) }}
    {% include toolbox_area_path(areaId, areaTemplateDirectory) with {'galleryId': galleryId} %}
    {{ wrap_macro.createWrapFoot(elementThemeConfig) }}
</div>