services:

    _defaults:
        autowire: true
        autoconfigure: true
        public: true

    ToolboxBundle\Tool\Install:
        arguments:
            $bundle: "@=service('kernel').getBundle('ToolboxBundle')"