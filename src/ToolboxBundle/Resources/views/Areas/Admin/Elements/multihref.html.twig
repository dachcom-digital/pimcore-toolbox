{{ pimcore_multihref(element.name,
{
    'width'      : element.width,
    'height'     : element.height,
    'title'      : element.title,
    'uploadPath' : element.uploadPath,
    'types'      : element.types,
    'subtypes'   : element.subtypes,
    'classes'    : element.classes,
    'class'      : element.class,
}) }}