{% if editmode %}

    <div class="container">
    <div class="row">
    <div class="col-xs-12">

{% endif %}

{{ pimcore_areablock('content', toolbox_areablock_config(null, true)) }}

{% if editmode %}

    </div>
    </div>
    </div>

{% endif %}
