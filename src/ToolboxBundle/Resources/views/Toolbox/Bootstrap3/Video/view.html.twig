{% if editmode %}

    {{ pimcore_vhs('video', {
        'attributes' : {
            'class' : 'video-js vjs-default-skin vjs-big-play-centered',
            'data-setup' : '{}'
        },
        'thumbnail' : 'content',
        'disableProgressReload' : true,
        'height' : 250

    }) }}

{% else %}

    <div class="video-inner">
        {% include '@Toolbox/Toolbox/Bootstrap3/Video/type-' ~ videoType ~ '.html.twig' %}
    </div>

{% endif %}